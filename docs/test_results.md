# 官方 Test Set 分數追蹤（n=1,100）

> 評測結束後網站仍可提交（每天 10 次額度）。本檔累積各 run 的官方 test 分數。
> 用途：① 把論文變異估計從 dev bootstrap 代理值升級為官方集合實測；
> ② 檢驗 val 上的結論（ensemble 死路、trade-off、圖譜等）在 test 上是否成立。
>
> 提交格式：`outputs/{run}_test.csv.zip`（`predict.py` 產生、`fetch_results.py --pack` 打包）。

---

## 已取得的 test 分數

| run | 架構 | V_MAE ↓ | V_PCC ↑ | A_MAE ↓ | A_PCC ↑ | 備註 |
|---|---|---|---|---|---|---|
| **e19_source_aware** | 31維L1 + source-aware | 0.6200 | 0.8663 | 0.9259 | 0.3566 | **正式提交** |
| macbert_s42 | 10維L1（≈E4, seed42） | 0.61 | 0.87 | 0.94 | 0.37 | 無增強 baseline |
| macbert_s1 | 10維L1（seed1） | 0.62 | 0.87 | 0.91 | 0.37 | 無增強 baseline |
| macbert_s2 | 10維L1（seed2） | 0.61 | 0.87 | 0.92 | 0.37 | 無增強 baseline |
| macbert_s3 | 10維L1（seed3） | 0.61 | 0.87 | 0.92 | 0.38 | 無增強 baseline |
| macbert_s4 | 10維L1（seed4） | 0.62 | 0.87 | 0.95 | 0.37 | 無增強 baseline |
| e18_l1_intensity | 31維intensity（無sa） | 0.61 | 0.87 | 0.93 | 0.37 | 2×2 第三格 |
| e19（predict.py 重跑） | 31維 + source-aware | 0.61 | 0.87 | 0.93 | 0.36 | **管線驗證**，見下 |
| e20_rank_aug | ranking-only 增強 | 0.63 | 0.87 | 0.93 | 0.37 | val 上已淘汰 |
| roberta_s42 | RoBERTa-wwm-ext base + 10維L1 | 0.62 | 0.86 | 0.92 | 0.38 | 換 encoder |
| robertaL_s42 | RoBERTa-wwm-ext **large** + 10維L1 | 0.67 | 0.86 | 0.91 | **0.39** | **val 判淘汰,test A_PCC 最高** |

---

## 無增強 baseline 的 test seed 變異（進行中）

目標：跑完 5 個 seed（macbert_s42/s1/s2/s3/s4），得到**官方 test 上真實的 seed std**，
取代目前在 dev 上 bootstrap 的代理估計（那是論文最容易被審稿人攻擊的但書）。

| seed | V_MAE | V_PCC | A_MAE | A_PCC |
|---|---|---|---|---|
| 42 | 0.61 | 0.87 | 0.94 | 0.37 |
| 1 | 0.62 | 0.87 | 0.91 | 0.37 |
| 2 | 0.61 | 0.87 | 0.92 | 0.37 |
| 3 | 0.61 | 0.87 | 0.92 | 0.38 |
| 4 | 0.62 | 0.87 | 0.95 | 0.37 |
| **mean±std** | **0.614±0.006** | **0.870±0.000** | **0.928±0.016** | **0.372±0.004** |

**✅ 已湊齊 5 seed，這是官方 test 集合上的真實 seed 變異**（取代 dev bootstrap 代理值）：

- **A_PCC = 0.372 ± 0.004，全距僅 [0.37, 0.38]。** 訓練隨機性對 arousal 排序的影響**極小**。
- 對照：E4（0.37）、E19（0.357）、5-seed baseline（0.372）三者全部落在這條 0.37±0.005 的窄帶裡
  → **arousal 上「哪個系統比較好」根本無從分辨**，val 上 E4/E19 的 0.026 差距確定是雜訊。
- V_PCC 五顆完全相同（0.870），再次顯示 valence 穩定飽和。

**⚠️ 誠實但書（論文要寫）**：leaderboard 只回報到小數點後 2 位，所以這裡的 std 受**捨入粒度**限制，
是真實 seed 變異的**上界估計**。即便如此，0.004 已經小到足以支撐「變異來自評估集大小、非訓練隨機性」的論點。

**論文用途**：這組數字直接把 `docs/experiments.md` §5.4 的第 4 點
（「排除訓練隨機性」）從 dev 代理**升級為官方 test 實測**，消滅審稿人最可能攻擊的但書。

---

## ✅ 管線驗證通過（重要）

`e19` 用 `predict.py` 從 checkpoint 重跑的 test 分數 = **0.61 / 0.87 / 0.93 / 0.36**，
對照原始正式提交 **0.6200 / 0.8663 / 0.9259 / 0.3566**（四捨五入到 2 位 = 0.62/0.87/0.93/0.36）：
- A_MAE / A_PCC / V_PCC **完全吻合**，V_MAE 差 0.01（MPS vs CUDA 的數值差，使用者已決定採用原始 0.6200）。
- ⇒ **`predict.py` 重跑管線與當初 Colab 推論等價**，後續所有用它產生的 test 分數皆可信。

---

## 💥 核心發現：2×2 ablation 在 test 上整個蒸發

val 上驅動我們提交決策的那套敘事（「31 維強度特徵有害、source-aware 有效」），在 test 上不成立：

| 系統 | 設定 | **A_PCC val (n=200)** | **A_PCC test (n=1,100)** |
|---|---|---|---|
| E4 | 10維L1，無 source-aware | 0.426 | 0.372 |
| E18 | 31維intensity，無 source-aware | 0.408 | 0.370 |
| E19 | 31維 + source-aware（**提交**） | **0.452** | **0.357** |
| E20 | ranking-only 增強 | 0.407 | 0.370 |

- **val 全距 0.045**（看起來 E19 明顯領先、E18 明顯落後）→ **test 全距僅 0.015**，且**排序完全打散**：
  val 上最高的 E19 在 test 上變**最低**，val 上被判「有害」的 E18 在 test 上與 E4 並列。
- 四個系統在 test 上全部落在 **0.357–0.372**，遠小於 n=1,100 的 A_PCC 95% CI 寬（0.103）。
- ⇒ **整個 2×2 消融的「結論」都是 val 上的抽樣雜訊。** source-aware、強度特徵、ranking 增強
  對 arousal **都沒有可偵測的真實效果**。這是論文 selection-overfitting 論點的最強實證：
  不只提交的那一顆選錯，是**整組據以做決策的排名都不可信**。

> **對論文的影響**：`docs/paper/outline.md` 中標為 ❌needs-evidence 的
> 「source-aware loss 改善 arousal」「31 維強度特徵有害」兩條 claim，
> test 證據顯示**兩者皆不成立** → 論文不得宣稱 source-aware 有效，
> 應改寫為「這些在 val 上看似有效的技巧，在 test 上都落在雜訊內」。

### 再一次 val→test 翻轉：RoBERTa-large

| 系統 | A_PCC **val** | val 當時判定 | A_PCC **test** |
|---|---|---|---|
| robertaL_s42 | 0.407（最差之一） | 「四指標全輸，確認淘汰」 | **0.39（全場最高）** |
| roberta_s42 | — | — | 0.38 |
| macbert（5-seed） | 0.42–0.45 | baseline | 0.372±0.004 |

- **val 上被判「淘汰」的 robertaL,在 test 上 A_PCC 反而最高。** 又一次排序翻轉。
- **但仍在雜訊內**：0.39 vs macbert 的 0.372,差 0.018 << n=1,100 的 CI 寬 0.103。
  且 robertaL 的 **V_MAE 0.67 是全場最差**(val 也最差 0.691)→ 整體不會贏。
- 論述紀律不變:**不能寫「robertaL 比較好」**。這是 selection-overfitting 的又一個實例——
  某個在 val 上被自信否決的模型,在 test 上「贏」了某個維度,而兩者其實都在雜訊裡。

**目前 test 上所有 run 的 A_PCC 全景**:落在 **0.357–0.39**（寬 0.033，仍 < CI 0.103）。
encoder 換 roberta 的兩顆(0.38/0.39)數值略高於 macbert 群(0.357–0.372),
但差距不足以在 n=1,100 上判定顯著。**arousal 天花板約 0.37–0.39,誰都突破不了。**

---

## 待提交清單（本機已備妥 zip）

`outputs/*_test.csv.zip`，依 `docs/remaining_experiments.md` P0-B 的價值排序：

1. ✅ macbert_s42、✅ macbert_s1
2. ✅ macbert_s2 / s3 / s4（seed 變異已算出 0.372±0.004）
3. ✅ e18_l1_intensity、✅ e20_rank_aug、✅ roberta_s42、✅ robertaL_s42
4. 🔲 e10_seed_ens（**驗證 ensemble 是否真的有害——最後一個高價值提交**）
5. 🔲 macbert_pseudo_s42（E13 校準↔排序 trade-off 是否在 test 成立）

之後 KG 消融的 18 個 run 訓練完，再各自提交。

---

## KG 消融（18 run）— 訓練完成，待提交 test

六條件 × 3 seed，`outputs/aug_{cond}_s{seed}_test.csv.zip` 已打包（各約 25KB）。

### dev 分數（mean±std，⚠️ dev 對增強失真，僅供參考）

| 條件 | V_MAE | V_PCC | A_MAE | A_PCC |
|---|---|---|---|---|
| N | 0.488±0.012 | 0.810±0.006 | 0.844±0.010 | 0.603±0.009 |
| A | 0.493±0.013 | 0.809±0.005 | 0.848±0.013 | 0.601±0.012 |
| C | 0.488±0.010 | 0.808±0.005 | 0.848±0.009 | 0.598±0.009 |
| E | 0.488±0.013 | 0.810±0.007 | 0.847±0.016 | 0.599±0.013 |
| F | 0.496±0.017 | 0.806±0.009 | 0.850±0.008 | 0.600±0.005 |
| F2 | 0.488±0.016 | 0.810±0.011 | 0.850±0.004 | 0.602±0.006 |
| **無增強 baseline (5 seed)** | — | 0.815 | 0.838 | **0.609** |

- **dev 上六條件全在雜訊內**（A_PCC 0.598–0.603，±0.01），且都略低於無增強 baseline。
- 但這符合預期：dev 與合成資料同風格 → 對增強失真（實驗 5 教訓）。**結論必看官方 test。**

### ✅ label-free 訊號：test 上的 arousal 預測 spread（對抗壓縮的直接證據）

arousal 壓縮（pred std 偏小）是本任務 A_PCC 低的核心病灶。不需 gold 標籤即可算：

| 條件 | test A_pred std |
|---|---|
| 無增強 baseline | ~0.76 |
| N / A / C | 0.82 / 0.81 / 0.82 |
| **E**（圖擴散） | **0.85** |
| **F**（＋錨定） | **0.85** |
| **F2**（＋放寬長度） | **0.89** |

- **單調趨勢且方向正確**：C（查表 0.82）< E（圖擴散 0.85）< F2（0.89）。
- 圖擴散 + 錨定 + 放寬長度**逐步撐開被壓縮的 arousal 分布**，正是設計意圖。
- ⚠️ spread↑ 不保證 PCC↑（實驗 5 教訓：撐開不等於排序對）→ **須上傳 test 驗證是否轉成 A_PCC**。

### 提交策略（18 run，2 天用完額度）

**必須全 18 個都提交**——單次提交比不出條件差異（n=1,100 的 A_PCC CI 寬 0.103），
只有拿到每條件 3 seed 的 test A_PCC mean±std，才能判斷 C vs E vs F2 的差距是否真實。
`outputs/aug_*_test.csv.zip` 全部備妥。

### 官方 test 分數（陸續回填）

| 條件 | seed | V_MAE | V_PCC | A_MAE | A_PCC |
|---|---|---|---|---|---|
| N | s1 | 0.62 | 0.87 | 1.03 | 0.34 |
| N | s2 | 0.64 | 0.86 | 1.05 | 0.36 |
| N | s42 | 0.61 | 0.87 | 1.12 | 0.36 |
| C | s1 | 0.62 | 0.87 | 1.03 | 0.35 |
| C | s2 | 0.64 | 0.86 | 1.07 | 0.38 |
| C | s42 | 0.61 | 0.87 | 1.14 | 0.38 |
| E | s1 | 0.62 | 0.87 | 1.03 | 0.36 |
| E | s2 | 0.65 | 0.86 | 1.06 | 0.38 |
| E | s42 | 0.62 | 0.87 | 1.08 | 0.38 |
| F | s42 | 0.62 | 0.87 | 1.08 | 0.38 |
| F2 | s1 | 0.63 | 0.87 | 1.07 | **0.40** |
| F2 | s2 | 0.63 | 0.86 | 0.96 | 0.38 |
| F2 | s42 | 0.62 | 0.87 | 1.08 | 0.38 |

| 條件 | A_PCC mean±std | A_MAE mean±std |
|---|---|---|
| **N（無種子詞）** | **0.353±0.012** | 1.067 |
| **C（VA 查表）** | **0.370±0.017** | 1.080 |
| **E（圖擴散）** | **0.373±0.012** | 1.057 |
| F（1 seed） | 0.38 | 1.08 |
| **F2（圖+錨定+真實長度）** | **0.387±0.012** | 1.037 |
| **無增強 baseline (5 seed)** | **0.372±0.004** | **0.928** |

### 🎯 定案：一個乾淨的成分分解（增強條件內部有梯度，但整體打不贏不增強）

三顆 seed 齊了，浮現一個**單調梯度**，且可乾淨拆解成分貢獻：

```
N   0.353   （不給種子詞）
 │   +0.017   ← 種子詞有用（給模型情緒詞錨點）
C   0.370   （VA 查表種子）
 │   +0.003   ← 圖結構【幾乎無貢獻】（E≈C，儘管連貫度 15.8×、spread 更大）
E   0.373   （圖擴散種子）
 │   +0.014   ← 風格錨定 + 真實長度分布【有貢獻】
F2  0.387   （圖 + 錨定 + match_real 長度）
```

**兩個可寫進論文的乾淨結論**：

1. **「情感知識圖譜」這個原本的主賣點，貢獻為零**：C（查表）→ E（圖擴散）只 +0.003，
   遠在 seed std（0.012–0.017）內。圖的連貫度 15.8 倍、arousal spread 更大，**都沒轉成排序提升**。
   增益全部來自「有種子詞」（N→C）與「風格錨定+長度」（E→F2），**與圖無關**。
2. **最強的 F2 仍打不贏「不增強」**：F2 A_PCC 0.387 vs baseline 0.372 只 +0.015（≈1 std），
   而 F2 的 **A_MAE 1.04 遠差於 baseline 0.93**（增強一律傷校準）。
   官方 4 指標 mean rank 下，**F2 會輸給不增強的 baseline**（A_MAE 大輸、A_PCC 僅平手級微贏）。

> **論文定調（誠實但完整的 negative result）**：
> 我們對圖引導可控生成做了徹底的成分消融（種子策略 × 圖結構 × 風格錨定 × 長度分布），
> 在 1,100 篇官方測試集上發現：**(a) 種子詞與風格/長度對齊有小幅內部效果，
> 但知識圖譜結構本身無貢獻；(b) 沒有任何生成增強變體能勝過「完全不增強」，
> 因為增強帶來的 arousal 排序微增被校準惡化（A_MAE +0.1）抵銷。**
> 這與 selection-overfitting 主線合流：arousal 上看似有希望的招數，
> 在大測試集 + 完整指標下全部站不住。
> **這是一篇有完整消融、誠實結論、且證據強度罕見（n=1,100 × 官方分數）的 shared-task 論文。**

### 💥 核心結論（C/E/F 已足以定調，F2 待補）

三件事在官方 test 上成立：

1. **圖結構對 A_PCC 沒有可測效果**：C 0.370 vs E 0.373，差 0.003，遠在 seed std（±0.01–0.02）內。
   儘管 E 的種子連貫度是 C 的 15.8 倍、test arousal spread 也更大（0.85 vs 0.82），
   **這些都沒有轉成 arousal 排序的提升**。
2. **增強完全沒提升 A_PCC**：C/E/F 全是 0.37，與無增強 baseline 0.372 **相同**。
3. **增強嚴重傷 A_MAE**：1.06–1.08 vs 無增強 0.93（+0.13～0.15）。
   ⇒ **實驗 5「A_PCC 撐開但 A_MAE 爆掉」的現象在 n=1,100 上完整重現**，且這次連 A_PCC 都沒撐起來。

> **對論文的意義（誠實但重要）**：「情感知識圖譜引導的可控生成能改善 arousal」這個原本要當主賣點的假說，
> **在官方 test 上不成立**——圖擴散不優於查表，增強不優於不增強，且都傷校準。
> spread 單調上升（C<E<F2）是真的、方向也對，但**撐開分布沒有轉成更好的排序**，
> 正是實驗 5b 早就警告過的。論文重心應誠實轉為：
> **「我們系統性地測試了圖引導生成的多個變體，在大測試集上證明它們對 arousal 均無效——
> 撐開 arousal 分布不等於改善其排序」**，與 selection-overfitting 主線合流成一個完整的 negative-result 故事。
> （待 F2 3 seed 補齊確認；若 F2 也 ~0.37，結論鎖定。）

---

## no-L1 消融（釘死「L1 詞典融合是否為正面貢獻」）— 完成

| seed | V_MAE | V_PCC | A_MAE | A_PCC |
|---|---|---|---|---|
| s1 | 0.62 | 0.87 | 0.91 | 0.34 |
| s2 | 0.62 | 0.87 | 0.91 | 0.37 |
| s42 | 0.61 | 0.87 | 0.95 | 0.38 |

| | A_PCC | A_MAE | V_PCC |
|---|---|---|---|
| **no-L1（3 seed）** | **0.363±0.021** | 0.923 | 0.870 |
| **有 L1 baseline（5 seed）** | **0.372±0.004** | 0.928 | 0.870 |

**結論：L1 詞典融合在 test 上不構成可測貢獻。**
- A_PCC 差 0.009，遠在 no-L1 的 seed std（0.021）內。
- V_PCC 兩者皆 0.87——**連 L1 對 valence 的 val 增益（0.870→0.880）也在 test 完全消失**。
- ⇒ **這是方向 A 最純粹的版本**：我們試過的每一個介入——source-aware、強度特徵、圖生成、ensemble、
  乃至最基礎、最受信任的 L1 詞典融合——在唯一可信的 test arousal 指標上，全部與「什麼都不做」不可區分。
